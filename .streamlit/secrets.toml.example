# Copy this file to .streamlit/secrets.toml and fill in a real password.
# Never commit the real secrets.toml to git.

APP_PASSWORD = "change-me"

# Optional, for the AI Assistant page's real LLM mode:
# ANTHROPIC_API_KEY = "sk-ant-..."
